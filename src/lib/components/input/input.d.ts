declare module "input" {
    interface InputProps {
        color?: string,
        bgColor?: string,
        borderColor?: string,
        size?: string,
        onClick?: fn,
        borderRadius?: string,
        border?: string,
        gap?: string,
        padding?: PaddingProps
    }
}